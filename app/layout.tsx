import type { Metadata } from 'next'
import { Inter, Playfair_Display } from 'next/font/google'
import './globals.css'
import Header from '@/components/Header'
import Footer from '@/components/Footer'

const inter = Inter({ 
  subsets: ['latin'],
  variable: '--font-inter',
})

const playfair = Playfair_Display({ 
  subsets: ['latin'],
  variable: '--font-playfair',
})

export const metadata: Metadata = {
  title: 'Lydia Décor - Décoration d\'intérieur élégante et moderne',
  description: 'Découvrez nos réalisations uniques en décoration d\'intérieur et notre collection d\'articles de décoration. Lydia Décor, votre partenaire pour un intérieur de rêve.',
  keywords: 'décoration, intérieur, design, mobilier, décoration moderne, Lydia Décor',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="fr" className={`${inter.variable} ${playfair.variable}`}>
      <body className="font-sans">
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  )
}
